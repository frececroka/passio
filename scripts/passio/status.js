(function () {
	'use strict';

	define(['angular'], function (angular) {

		var status = angular.module('passio.status', []);

		status.factory('PassioStatus', function () {
			return {};
		});

	});
}());
